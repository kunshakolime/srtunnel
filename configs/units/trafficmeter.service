[Unit]
Description=trafficmeter
After=network.target

[Service]
Type=simple
# loader opens traffic_meter.bpf.o by relative path — must run from bin/
WorkingDirectory=/opt/srtunnel/bin
ExecStart=/opt/srtunnel/bin/traffic_meter_user
Restart=always
RestartSec=2

[Install]
WantedBy=multi-user.target